/*
The server will pick up the clients sent information based off the 
file name, MAC Header, IP Packet header, UDP header, message, end of message
*/

//#include "NET.h"
//DO NOT USE STRING FUNCTIONS 
//RECIEVER

//When there is a successful listen, read the header first

//Recieve the parts of the code and parse into a message

//Send final message that is recieved

//If final is successful, create file based off extension(?)
//Make the file granted it is perfectly(?) sent